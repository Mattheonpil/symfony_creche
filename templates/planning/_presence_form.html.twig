<form data-action="submit->planning-editor#submit" data-planning-id="{{ planning.id }}">
    {{ form_row(form.absence) }}
    {{ form_row(form.actual_arrival) }}
    {{ form_row(form.actual_departure) }}
    <button type="submit" class="btn btn-primary">Valider</button>
</form> 
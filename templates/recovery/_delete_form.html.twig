<form method="post" action="{{ path('app_recovery_delete', {'id': recovery.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recovery.id) }}">
    <button class="form-button form-button--delete">Delete</button>
</form>

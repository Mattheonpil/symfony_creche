{% extends 'base.html.twig' %}

{% block title %}Recovery{% endblock %}

{% block body %}
    <h1>Recovery</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recovery.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ recovery.name }}</td>
            </tr>
            <tr>
                <th>First_name</th>
                <td>{{ recovery.firstName }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ recovery.phone }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_recovery_index') }}">back to list</a>

    <a href="{{ path('app_recovery_edit', {'id': recovery.id}) }}">edit</a>

    {{ include('recovery/_delete_form.html.twig') }}
{% endblock %}
